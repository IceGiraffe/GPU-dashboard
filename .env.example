TOTAL_GPU_COUNT=64
DISPLAY_TIME_ZONE=Asia/Shanghai
# 推荐：部署到 Vercel 后接入 Postgres（Neon / Supabase / Prisma Postgres 等）
# POSTGRES_URL=postgres://...
# DATABASE_URL=postgres://...
#
# 兼容旧方案：也支持 Blob 持久化
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx
